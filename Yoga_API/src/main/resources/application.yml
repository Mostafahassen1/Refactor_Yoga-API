spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/yoga
    username: yoga
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
